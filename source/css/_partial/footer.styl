.site-footer {
  // border-top: 1px solid color-border;
  padding-top: 1.5em;
  font-size: font-size-smaller;
  font-weight: 300;
  color: lighten(color-grey, 20%);
}

.footer-wrap {
  text-align: center;
  padding: 0.8em 0;
  line-height: 1.4;
  // display: flex;
  // justify-content: space-between;
  // flex-wrap: wrap;
  p {
    margin: 0;
  }
  a {
    color: color-grey;
    font-weight: bold;
  }
}

.i-love {
  &:after {
    use-icon-as-content(icon-love, color-green);
  }
}
